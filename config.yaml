config:
  backup: true
  create: true
  dotpath: dotfiles
dotfiles:
  f_zshrc:
    src: zshrc
    dst: ~/.zshrc
  f_starship.toml:
    src: config/starship.toml
    dst: ~/.config/starship.toml
  f_gitconfig:
    src: gitconfig
    dst: ~/.gitconfig
profiles:
  main:
    dotfiles:
    - ALL
